server:
  port: 8888

spring:
  application:
    name: cloud-sso
  profiles:
    active: dev


  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yaml
    sentinel:
      transport:
        dashboard: localhost:4444
      web-context-unify: false
      enabled: true
feign:
  sentinel:
    enabled: true